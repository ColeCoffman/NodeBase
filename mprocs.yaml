procs:
  next:
    cmd: ["npm", "run", "dev"]

  inngest:
    cmd: ["npm", "run", "inngest:dev"]

  prisma_server:
    cmd: ["npx", "prisma", "dev"]

  prisma_studio:
    cmd: ["npx", "prisma", "studio", "--browser", "none"]
