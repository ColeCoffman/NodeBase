procs:
  next:
    cmd: ["npm", "run", "dev"]

  inngest:
    cmd: ["npm", "run", "inngest:dev"]

  prisma_server:
    cmd: ["bash", "./scripts/prisma-dev.sh"]

  prisma_studio:
    cmd: ["npx", "prisma", "studio", "--browser", "none"]
